# TrackEditor Levels

Dieser Ordner ist fuer exportierte Laufzeitdateien wie `level01.js` vorgesehen.
Der Editor erzeugt diese Dateien lokal im Browser per Download; er muss hierfuer keinen
Webserver und kein `fetch()` verwenden.
